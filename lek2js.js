var box = document.getElementById('3');

function sh(x){
    box.value += x;
    
    if (x=='c'){
        box.value = '';
}

function wynik() {
    x = box.value;
    x = eval(x);
    box.value = x;
    if ()
}
function dodawanie() {
    x = box.value;
    x = eval(x);
    box.value = x;
    if ()
}
function odejmowanie() {
    x = box.value;
    x = eval(x);
    box.value = x;
    if ()
}
function mnozenie() {
    x = box.value;
    x = eval(x);
    box.value = x;
    if ()
}
function dzielenie() {
    x = box.value;
    x = eval(x);
    box.value = x;
    if ()
}